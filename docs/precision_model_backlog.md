# 정밀 추정 모델 — 작업 백로그 / 메모

> 거래내역 기반 손익·박스권 추정 모델 고도화를 위한 누적 메모.
> 세션이 바뀌어도 누락되지 않도록 여기에 기록한다.

## 데이터 수급 현황

- **6/18(목)~6/24(수)**: 빗썸 거래내역 업로드 완료 (실측 캘리브레이션 1차)
- **6/25~6/30**: 거래내역 업로드 예정 → 2차 데이터 포인트 (아직 미입수)
- 일봉/분봉 데이터셋(zip): BTC·USDT 일봉, 5분봉, 환율, 김프, monthly_labels 포함
- **데이터셋 시드 갱신(2026-06-29)**: `data/dataset_seed.zip` → BTC 일봉 2026-06-28,
  5분봉 ~2026-06-29 20:10, monthly_labels에 2026-06 실현 라벨 포함까지 최신화.

## ⚠️ 분석 대상: 2026-06 박스권 예측 미스 (label_ok=0)

신규 시드의 monthly_labels에서 2026-06 라벨이 **예측 실패**로 기록됨:
- ref_price 108,445,000 / daily_sigma 0.013855 / monthly_sigma 7.589%
- 1σ 박스 [100,519,952 ~ 116,994,863], 2σ 박스 [93,174,058 ~ 126,218,802]
- **contain_1s 3.6% / contain_2s 67.9%** (목표 1σ≥68.3% 한참 미달), **하방 이탈 9일**,
  max 하방 이탈 2.45%, 실현 변동성 10.623% (예측 7.589%의 약 1.4배)
- 즉 6월은 예측보다 변동성이 크고 하방으로 쏠려 박스가 가격을 못 담음.

→ 다음 세션 "박스권 1σ 정교화"의 핵심 케이스. 거래내역(6/25~30) 확보 시 함께 분석:
  (a) 실현 변동성이 예측 대비 왜 1.4배였는지 (σ 추정 보수성/모멘텀 누락?),
  (b) 하방 비대칭(9일 하방 이탈)을 σ_dn 배수/박스 하단으로 어떻게 흡수할지,
  (c) 실측 체결가 분포 vs 박스 위치 대조 → 1σ 폭·중심 조정폭 산출.

## ✅ 1차 캘리브레이션 확정 — 2-포인트 (누락 보정 후, 2026-06-30)

> 6/18~24 거래내역 1차 업로드분에 **6/24 종일(168건) 누락**이 있었음 →
> 6/24 거래내역 파일을 추가 병합(중복 1건 제거)해 정정. 공식 순익도 사용자 정정 반영.

| 기간 | 일수 | 봇거래(정정) | 공식순익 | pnl율 | BTC왕복 | 일당왕복 | 실효마진 | 평균변동 |
|---|---|---|---|---|---|---|---|---|
| 6/18~24 | 7 | 1.0335억 | 92,952원 | **0.0899%** | 335 | 47.9 | 0.261% | 3.18% |
| 6/25~29 | 5 | 0.8793억 | 75,678원 | **0.0861%** | 350 | 70.0 | 0.302% | 2.97% |

- **pnl율 ≈ 0.0880% ± 0.0019%p** (거래대금 대비 순익, 리워드 별도). 두 주 거의 일치 → 신뢰 가능한 앵커.
  - 거래량 1억당 주간 순익 ≈ 8.6~9.0만원.
- **동일 전략 검증 완료**: 실효마진 0.26~0.30%, pnl율 거의 동일.
- **거래량 동인 = 변동성·일당 왕복빈도** (일당 왕복 47.9 vs 70.0). 거래량 추정 = 일당왕복×일수×요일가중.
- 주의: 거래내역 정산금액+잔여 MTM으로 순익을 역산하면 다일(多日) 인벤토리 변동 탓에 부정확
  (6/18~24가 −22만으로 공식 +9.3만과 반대로 나옴) → **순익은 반드시 공식 실측값 사용**.
- 7/1 (6/30 입수) 시: 6월 완전체로 월말 효과 보정 + 3번째 포인트로 pnl율 분포 확정.

## 실측 캘리브레이션 결과 (6/18~6/24 기준 — 아래는 누락 보정 전 초기값, 참고용)

| 항목 | 값 |
|---|---|
| 주간 총 거래대금 | 0.863억 (BTC 0.743 + USDT 0.120) |
| 거래건수 | BTC 603, USDT 193 (총 796) |
| BTC 왕복 | 약 279회 / USDT 약 91회 |
| 실효 그리드 마진 | BTC 0.168% / USDT 0.096% |
| 순 매매수익(수수료 차감, 리워드 별도) | 101,173원 |
| 거래대금→수익 변환율 | 0.1172% |
| 봇/시장 점유율 | 평일 0.015% / 주말 0.009% |
| 주말/평일 거래량 비율 | 0.60x |
| 투입자본 합계 | 32,074,297원 |
| 주간 ROI(그리드만) | 0.3154% (월 1.37% / 연 16.4%) |

## 다음 세션 작업 항목 (6/25~30 데이터 도착 시)

1. **모델 캘리브레이션 고도화**
   - 2차 실측으로 pnl_rate를 단일값 → 분포/범위로 확장
   - 시장 변동폭(일봉 range%)과 pnl_rate 상관 분석 → 변동성 조정 계수
   - 6/1~6/17 추정 구간 소급 보정

2. **박스권 설정 정교화 — 특히 1시그마**
   - 실측 체결가 분포로 실제 활동 구간 역산 → 설정 박스권 최적성 검증
   - monthly_labels.csv의 daily_sigma / realized_sigma_pct로 적정 σ 재계산
   - 가격이 박스권 어느 위치에서 거래됐는지 히트맵, 상·하단 조정폭 제안

3. **ML 고도화 검토**
   - 피처 후보: 일봉 변동폭, 시장 거래대금, 김프, 요일
   - 예측 대상: 주간 봇 거래대금, 왕복횟수, 순익
   - 데이터 포인트가 아직 2개뿐 → 단순 회귀부터 시작하는 로드맵

4. **리워드 연동 총손익 모델**
   - 그리드 수익 + 리워드 티어 합산

5. ~~**USDT 박스권 σ를 백엔드 예측에 연결**~~ ✅ 완료(2026-06-30, usdt_snap 연결 — 아래 "① 1차 선행 작업 결과" 참조)
   - 현재 USDT 박스권 σ는 프론트엔드 상수(`index.html` `USDT.sigma_pct = 2.2`)로 고정 →
     백엔드의 USDT 예측(`predict_as_of(symbol="USDT")`, `config.py`의 σ_blend = (1-w)·σ_usdt + w·σ_fx)
     결과와 연결되어 있지 않음.
   - **정정 메모**: "USDT 분봉 미수집"은 사실이 아님. USDT 분봉은 정상 수집됨
     (`utils/minute_data.py` MARKETS에 KRW-USDT 포함, 시드/증분 수집 동작).
     다만 KRW-USDT 5분봉 히스토리가 2024-06~로 BTC보다 짧아 σ 추정 시 환율 σ와 블렌딩해 보완하는 구조.
   - 할 일: BTC처럼 USDT 박스권도 백엔드 `predict_as_of` 결과(σ 포함)를 받아오도록 프론트 연결.
     `index.html`의 `updateUsdtModel`이 상수 σ 대신 백엔드 스냅을 사용하도록 변경.

## 미스터리 쇼퍼 검수 (2026-06-28, 7월 예측 모드) — 처리 현황

수정 완료:
- (CRITICAL) 간격 추정 자동 트리거가 `daysLeft>=8`(이번달)이라 7월 예측 시점에 안 돌던 문제 →
  `nextMonthMode`로 반전 + `fetchIntervals`가 `target_month` 전달.
- (HIGH) USDT '수정 박스 적용'이 구 localStorage 키에 저장돼 새로고침 시 소멸 → 월별 키로 통일.
- (HIGH) 간격 추정 힌트 문구 부등호 반대 정정.
- (MEDIUM) BTC 현재가 폴백가=0을 '0원'으로 노출 → '가격 확인 불가'.
- (HIGH/설계) 모니터 탭이 다음달 예측 모드에서 빈 화면 → 백엔드 `monitor` 블록 신설(진행 중인
  현재 월 기준 별도 예측·측정), 프론트 모니터 함수 3종을 `D.monitor.*`로 분리. 예측 탭(다음달)과
  모니터 탭(진행 중 현재 월)이 독립적으로 올바른 월을 표시.

2차 수정 완료(MEDIUM/LOW):
- (MEDIUM) 포트폴리오 박스권 범위 셀(btc-inner-range2/outer-range2) 정적 가짜값 → '─' placeholder.
  (나머지 셀은 onload updateMetrics가 갱신하므로 stale 아님)
- (MEDIUM) 거래량 목표 자동추천 해제 시 슬라이더 원복 — 진입 전 값 백업 후 목표 0 복귀 시 복원.
- (MEDIUM) 데이터 현황: 합성 폴백 '⚠ 합성' 배지(캐시 파일 부재로 판별), 실시간 에러 카드를
  '폴백/캐시'와 분리해 '⚠ 수집 실패'(빨강)로 표기, catch 시 표 본문·실시간 카드도 실패 상태로 통일.
- (LOW) capital/prevVol 입력 상한 추가(1,000억 / 1e13).
- 검토 결과 비이슈: rewardNextTier 초기 공백은 실제로 onload에서 onPrevVolChange() 호출되어 정상.

잔여(보류):
- (LOW) USDT 차트 '월초' 라벨이 다음달 모드에서 의미상 '예측 시작 기준'과 다름 — 표기 개선 여지(영향 경미).

## BTC 3모드(일반/부스트/듀얼) — 개념정의 & 앱 준비 (2026-06-29)

- 개념정의 문서: `docs/btc_trading_modes.md` (3모드 목적·목적함수·운영구간·USDT 비유·비교표).
- 듀얼모드 구현: `utils/interval_optimizer.py`에 `dual` 산출 추가
  (1σ 밴드=부스트/volume + 2σ 밴드=일반/net, 자본 `dual_inner_ratio`(기본 0.6) 분할,
  거래량 합산 후 리워드 1회·상한 적용). `_combine_dual` 헬퍼.
- `/api/intervals` 응답에 `dual` 포함(시그니처 변경 없음), `index.html` 간격 패널에 듀얼 행 추가.
- **확정 대기(거래내역 후)**: 부스트 매도 하한/일반 상한, `dual_inner_ratio` 기본값,
  `VOLUME_SIM_CALIB`를 실측 왕복·체결가 분포로 재보정. (docs/btc_trading_modes.md "확정 대기" 참조)

## ✅ 최종 고도화 — 3-포인트 캘리브레이션 확정 (2026-07-01, 6/30 입수)

| 기간 | 일수 | 봇거래 | 공식순익 | pnl율 | 실효마진 | 평균변동 |
|---|---|---|---|---|---|---|
| 6/18~24 | 7 | 1.0335억 | 92,952 | 0.0899% | 0.261% | 3.18% |
| 6/25~29 | 5 | 0.8793억 | 75,678 | 0.0861% | 0.302% | 2.97% |
| 6/30 | 1 | 0.0952억 | 7,777 | 0.0817% | 0.438% | 2.45% |
| **6/18~30 거래량가중** | 13 | **2.0080억** | **176,407** | **0.0879%** | — | — |

- **pnl율 앵커 = 0.088%** (거래대금 대비 그리드 순익, 리워드 별도). 3-포인트 0.082~0.090%로 매우 안정.
  → `config.GRID_NET_RATE_OBSERVED = 0.00088`로 박제(모델 순익 sanity-check 앵커).
- **약한 양의 관계**: 변동성↑ → pnl율↑ (3.18%/0.090%, 2.97%/0.086%, 2.45%/0.082%). 단조.
- **월말 효과**: 6/30 거래량 0.0952억(주간 일평균의 ~55%로 급감), 왕복당 마진은 오히려↑(0.438%),
  순익률은 −7%(거래량 급감 탓). → `config.MONTH_END_VOLUME_DISCOUNT = 0.55`. 월간 거래량 추정 시 말일 디스카운트.
- **MTM 역산 순익은 사용 안 함**(다일 인벤토리 변동으로 부정확) — 공식 순익만 사용.

### 박스 중심 드리프트 (5개월 라벨 분석) — 모멘텀 예측 기각
- ret_prev31d ↔ 중심편차 상관 ≈ 0 (corr 0.07). "모멘텀으로 중심 보정" 가설 **데이터 기각**.
  (2026-03: −24% 후 +5.6% 반등 / 2026-02: −8% 후 −13.7% 지속 → 부호 예측 불가)
- 실패월(2·6월) |중심편차| 13% vs 성공월 4.6%. 실패 = 예측불가 큰 추세이동.
- → **강건성 대응**: σ_dn 비대칭 확대 + 데드존 리셋(DEAD_ZONE_RESET_DAYS=8) 사후대응. 표본 누적 후 재검증.

### 남은(차기) 과제
- 3모드 간격 정밀 캘리브레이션: 실측 0.2%/0.3% 전략이 옵티마이저 boost~normal 사이 → 모드별 탐색범위 미세조정.
- pnl율 변동성 조정계수: 포인트 누적 시 변동성↔pnl율 회귀로 동적화.
- VOLUME_SIM_CALIB 재검증: 실측 거래량 vs 시뮬 추정 대조(다음 월 데이터).

## 1차 선행 작업 결과 (2026-06-30)

### ① 2026-06 박스 미스 진단 — "중심(레벨) 문제" (σ폭 아님)
- 예측 중심 108,445,000(5/31 종가) vs 실제 6월 평균종가 95,145,586 (**−12.3%**).
- 1σ 하방이탈 28/29일, 2σ 하방이탈 10일. 단 실현 종가 표준편차 ±3.3% < 예측 1σ폭 ±7.6%.
- **결론**: σ폭은 충분히 넓었음. 문제는 박스 중심이 전월 종가에 고정돼 평균회귀를 가정한 것.
  6월은 −12% 지속 드리프트(ret_prev_31d −4.7% 모멘텀 존재했음).
- **1σ 정교화 방향(확정)**: σ폭 확대가 아니라 **중심에 드리프트/모멘텀 반영**(또는 σ_dn 비대칭 확대).
  그리드 관점: 중심이 높으면 한 달 내내 하락매수→데드존→인벤토리 누적→MTM 손실(순익 역산 −22만 원인).
  7/1 3-포인트로 보정폭 정량화.

### ③ USDT 박스 σ 백엔드 연결 — 완료
- `/api/dashboard`가 `usdt_snap`(predict_as_of symbol="USDT") 제공: target/ref/sigma_pct/1σ·2σ 밴드.
- `index.html updateUsdtModel`이 상수 2.2% 대신 백엔드 σ 사용(미연결/표본부족 시 상수 폴백).
  라벨에 σ 값·출처(예측σ/기본σ) 표시. 실측 예: 2026-07 USDT σ ≈ 4.3%(상수 2.2%보다 높음, FX 블렌딩).

### 리워드 처리 방침 (변경)
- 리워드 **정산 실측은 사용하지 않음** — 계정 내 전략 외 거래에 따른 리워드가 혼입되어 부정확.
- **멤버십 기준 부여비율(티어 요율)에 따른 추정값**으로 진행. (총손익 모델도 이 추정 리워드 사용)

## USDT 트리플 오버레이 재구성 (2026-07-05)

- USDT 부스트/일반 모드 + 간격 조정 기능(스테퍼·적용버튼) **폐지**.
- 고정 트리플 전략으로 전환: **1σ 1/1원(T1, 거래량) + 1.5σ 1/2원(T2, 균형) + 2σ 1/3원(T3, 수익)**
  동시 중복 운영. 자본 3등분, 리워드는 합산 거래량 1회 적용.
- 백엔드: `interval_optimizer`가 USDT에서 `triple` 반환(탐색 없음). 프론트: 시뮬레이터/포트폴리오
  카드·P&L 3행/존테이블/도넛/차트(1.5σ 라인) 전면 3-leg 재구성. BH 시뮬은 T1/T3 별칭으로 유지.
- **캘리브레이션 대기**: leg 활동률(1.0/0.82/0.64 잠정 — 1.5σ는 선형보간), 7월 실거래로 재보정.
  트리플 전략 실측 pnl율도 기존 0.088% 앵커와 별도로 추적 필요(전략 구조가 바뀌므로).
- **최적 자본배분 추정 추가(2026-07-05)**: leg 선형성 이용 가중치 격자 탐색(5% 스텝·최소 10%).
  거래량 목표 있으면 '목표 충족 중 순익 최대', 없으면 '순익 최대'. 백엔드 `triple.alloc` +
  프론트 실시간 추천/적용(`usdtAllocWeights`). 배분 추천의 실측 검증도 7월 캘리브레이션 대상.

## 관련 수정 이력

- **2026-06**: 월말 D-8부터 USDT/KRW 탭도 다음달 박스권을 표시하도록 수정
  (`getPredictionContext()` 헬퍼 추가, 원본 박스 스냅샷 키·라벨·차트 x축을 대상 월로 전환).
  단, USDT 박스 값 자체는 여전히 프론트 상수 σ 기반 → 5번 항목 완료(usdt_snap 연결).

## 미스터리 쇼퍼 2차 검수 (2026-07-05, 20 페르소나) — v0.8.0 배포 전 처리

수정 완료(배포 차단급):
- (CRITICAL) CHANGELOG v0.5.8에서 정체 → 0.5.9~0.7.6 소급 요약 + v0.8.0 작성.
- (HIGH) docs/가 make_patch·자동업데이트에서 누락 → 양쪽 SRC_DIRS에 추가(매뉴얼 배포).
- (HIGH) 갱신 시드 영구 미적용 → 시드 일봉 신선도 비교 후 자동 적용, skip 시 .imported 처리(UI 오표시 해소).

수정 완료(HIGH/MEDIUM):
- BH calcCombined 1σ 이중계산(옛 링 가정) 제거, buildGridLadder 폴백 동일 수정.
- BH 수동 봇자본 조정 → usdtAllocWeights 역산 유지(무통보 롤백 방지) + 트리플 카드·P&L 연동.
- buildUSDTChart 재귀 이중 렌더/가격 갱신 2중 체인 제거(_chartUSDT 설정 순서).
- 오프라인 첫 방문 orig 오염 — isLive일 때만 localStorage 저장. orig 라인 u2/l2 가드.
- _ivTripleRows 문자열 부호 판정("+-0.0만") → 숫자 기준.
- 배분 추정: 잔여목표(합산목표−BTC 예상거래량) 사용, 적용/복원 후 자동추천 재계산,
  균등≈최적 시 버튼 깜빡임 방지(개선 1만원 미만 → 균등 유지 권장).
- estimatePortfolio 리워드 계좌 300만 캡 반영(자동추천 서열화 왜곡 방지).
- volTarget 변경 시 간격 패널 백엔드 배분 행 재산출.
- 백엔드: 시뮬 ref=박스 중점(1450 고정 편향 해소), volume objective infeasible 폴백 net 비교,
  BTC 캐시 키 vol_target 정규화, CLI 에러 가드.
- 카피: "4전략"→5전략, USDT 패널 헤더 트리플화, 모니터 리스크 가이드 leg 기준, 툴팁,
  README "보류 모듈" 정정. 트리플 카드 모바일 1열(미디어쿼리 적용). patch.bat 변경이력 15줄 표시.

이연(백로그):
- (설계) 진행 중인 오늘 캔들이 containment에 포함 — '진행 중' 별도 표기 검토.
- (설계) USDT 월 원본 박스를 접속일 실시간가 대신 백엔드 예측(usdt_snap) 앵커로 전환.
- 2-leg만 유효할 때 배분 추정 일반화 / 모니터 블록 glines / USDT 차트 범례 축약(legendgroup) /
  모드 판정 3중 중복 통일 / 브라우저-서버 시간대 경계 / 합성 배지 첫 요청 오탐(검사 시점 이동) /
  P&L 리워드 캡 발동 시 행별 차감 표시 / 프론트·백엔드 배분 목적함수 차이(MTM 유무) 캡션.
- 검증 통과 확인: m-스텝 매핑(ref 무관 정확), 배분 선형성, 리워드 이중계산 없음(수학 동일성),
  BTC 3모드 회귀 무손상, 7/5 모드 라벨 정합, 반응형(트리플 grid 제외) 전반.
