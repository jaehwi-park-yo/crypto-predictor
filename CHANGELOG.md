# Changelog

## Unreleased
- **[신기능] MTM 서킷브레이커**: 가상 그리드 미실현 손실이 자본 2% 초과 시 모니터 탭에서
  신규 매수 정지 권고(매도 유지, 1% 회복 시 재개). 103개월 백테스트 — 평균 손익 중립,
  최악월 −222만→−102만. 백테스트: reports/mtm_circuit_backtest.py, 설계: docs/mtm_circuit_breaker.md

## v0.8.0 (2026-07-05)
### USDT 트리플 오버레이 전환 + 최적 자본배분 + 전면 검수
- **[전략 재구성] USDT 트리플 오버레이**: 부스트/일반 모드와 매수·매도 간격 조정 기능 폐지.
  예측 박스권 기반 3전략 동시 중복 운영으로 전환 — T1(1σ, 1/1원, 거래량·리워드) +
  T2(1.5σ, 1/2원, 균형) + T3(2σ, 1/3원, 스프레드 수익). 리워드는 합산 거래량에 1회 적용
- **[신기능] USDT 최적 자본배분 추정**: leg 선형성 기반 가중치 격자 탐색(5% 스텝·leg 최소 10%).
  거래량 목표 설정 시 "잔여목표(합산목표−BTC 예상거래량) 충족 중 순익 최대", 미설정 시 순익 최대.
  프론트 실시간 추천·적용/균등복원 + 백엔드 `/api/intervals` `triple.alloc`(`vol_target` 파라미터)
- **[신기능] BTC 3모드 체계화**: 일반(수익)·부스트(거래량)·듀얼(1σ 부스트+2σ 일반 자본분할) 산출
- **[신기능] 데이터 현황 탭**: 일봉/분봉/거시지표 보유내역(봉·기간·행수)·실시간 수집 상태·시드 현황.
  데이터셋 내보내기 버튼 이동. `docs/데이터셋_적용_매뉴얼.txt` 동봉(패치에 docs/ 포함 시작)
- **[개선] USDT 박스 σ 백엔드 연결**: 상수 2.2% → 예측 σ(FX 블렌딩, 실측 ~4.3%) 사용, 출처 라벨 표시
- **[개선] 모니터 탭 월 분리**: 월말 다음달 예측 모드에서도 모니터는 진행 중인 현재 월 실측 표시
- **[수정] 미스터리 쇼퍼 검수(20 페르소나) 반영**:
  BH 시뮬 1σ 이중계산(봇수 50%·거래량 19% 과대) 및 수동 봇자본 무통보 롤백 수정 ·
  오프라인 첫 방문 시 월 원본 박스가 폴백가(1401원)로 오염되던 문제(실시간가일 때만 저장) ·
  USDT 차트 이중 렌더(재귀) 및 가격 갱신 시 2중 렌더 체인 제거 ·
  갱신 시드가 기존 설치본에 영구 미적용되던 문제(시드 신선도 비교 후 자동 적용) ·
  간격 추정 "+-0.0만" 부호 표기 · 자동추천 리워드 300만 계좌 캡 미반영 ·
  배분 적용 시 자동추천 재계산 누락 · 시뮬 ref 고정(1450)으로 인한 거래량 ~3% 편향 ·
  "4전략" 표기(실제 5전략) 등 구시대 카피 일괄 정리 · 트리플 카드 모바일 1열 미적용
- **[데이터] 시드 2026-06-30(6월 마감)까지 갱신** + 6월 실거래 3-포인트 pnl율 캘리브레이션
  (`GRID_NET_RATE_OBSERVED=0.088%`, 월말 거래량 디스카운트 0.55)

## v0.6.5 ~ v0.7.6 (2026-06-19 ~ 2026-06-28) — 소급 요약
> 이 구간은 CHANGELOG 기록이 누락되어 git 이력 기반으로 소급 정리.
- **1분봉 파이프라인 전환**: 기동 시 1m 롤링 수집, 1m 단일소스→해상도별 집계(σ=5m·거래량=1m),
  페이지네이션 KST→UTC 수정, 5m 과소측정 보정계수 실측 재보정(2.7→2.1)
- **최적 매수/매도 간격 추정기 신설**: 1분봉 단일인벤토리 시뮬 기반 부스트/일반 간격 산출
  (`/api/intervals`, BTC 탭 UI 패널), USDT 매수 1원 고정 최적화, 월별 캐시
- **1m 포함 시드 배포** 및 부스트 거래량 목적함수·초촘촘 탐색·실측 보정계수(`VOLUME_SIM_CALIB=1.6`)

## v0.5.9 ~ v0.6.4 (2026-06-17 ~ 2026-06-19) — 소급 요약
- v0.5.9: 수정 박스 적용 버튼(BTC·USDT 모니터 탭)
- v0.6.0~0.6.1: 클라이언트 P&L 계수·USDT 매도간격 민감도 5분봉 실측 재보정
- v0.6.2~0.6.4: 투입자본 +백만/+천만 버튼, 목표거래량 기반 BTC비중 자동산출 개선,
  USDT 간격 적용 시 추천 재연동

## v0.5.8 (2026-06-17)
### 정밀 검수 — 연동수치 버그 수정 & 부스터 UX 개선
- **[CRITICAL] 거래량 목표 자동추천 BTC 외부존 모델 불일치**: `estimatePortfolio`가 outer를 `combined=false`로 계산해 P&L표(`combined=true`)와 거래량이 어긋남 → 추천 상태줄과 P&L 총합 불일치. `calcBtcStrat(..., 'outer', true)`로 통일
- **[버그] `calcUsdt` 0봇 나눗셈**: `box < buy`일 때 봇수 0 → 봇당자본 Infinity/NaN 전파. `Math.max(1, ...)` 가드 추가
- **[버그] CSS `--card2` 미정의**: 봇자본 단위 버튼·표시단위 입력칸 배경이 투명하게 렌더 → `:root`에 `--card2:#2b2f3a` 정의
- **[모델] 리워드 캡 계좌 단위화**: 기존 전략별 300만원 캡(이론상 최대 1,200만원) → 계좌 합산 후 300만원 캡 적용 (Bithumb 실제 정책 일치)
- **부스터 ON 상태 가시성**: 채워진 배경+✓ 배지+`aria-pressed`로 ON/OFF 명확화
- **부스터 간격 텍스트 동기화**: 사이드바 안내(`sb-int-note`)·BTC 차트 그리드 라벨이 부스터 상태 반영. 부스터 토글 시 차트 재빌드(BTC 탭 활성 시)·거래량 목표 재추천
- **사다리 단위 컨트롤 명확화**: "봇자본 조정단위"→"봇자본 증감폭"(실제 자본 변경), "봇자본 표시단위"→"사다리 표시 반올림 단위"(표시만)로 구분 + 툴팁 추가. 사다리 하단 단위 표기도 동적화

## v0.5.7 (2026-06-17)
### 기능 추가 — 거래량 부스터 모드 (전략 D)
- **배경**: 실제 BTC 1일 거래량이 시뮬레이션 대비 약 8×이하 (실측 350만원/일 vs 시뮬 2,500만원)
  - 원인: 시뮬레이션 기준 일중변동 4.2% vs 현재 실제 변동 0.52% (저변동 장세)
  - 전략 D (매수0.2%/매도0.3%): 사이클 구간(0.5%)을 실제 HL에 맞춰 거래량 최대화
- **거래량 부스터 토글 버튼** (`⚡ 거래량 부스터 OFF/ON`) 사이드바 거래량 목표 아래 추가
  - OFF: 기본 전략 (1σ 매수0.5%/매도1.0%, 2σ 매수0.3%/매도0.5%)
  - ON: 부스터 전략 (1σ/2σ 모두 매수0.2%/매도0.3% 통일)
- **동적 계수 스케일링** (`calcBtcCoefScaled`):
  - 거래량: `vol × baseCycle / newCycle` — 1σ 3.0×, 2σ 1.6× 증가 반영
  - 그리드수익: `grid × (sell_new/cycle_new) / (sell_base/cycle_base)` — 0.9×/0.96× 반영
- **봇수 동적 갱신**: `btcZoneBots()`가 `btcInterval(zone).buy` 참조 → 부스터 ON 시 0.2% 간격으로 봇수 재계산
- **BTC 간격 표시 동적화**: 4개 하드코딩 셀에 ID 부여(`si-btci-int`, `si-btco-int`, `btc-inner-int`, `btc-outer-int`), `updateMetrics()`에서 동적 업데이트
- **연동 검증**: `toggleBtcBooster()` → `updateMetrics()` + `updatePnlTable()` 연쇄 호출로 전체 재계산

## v0.5.6 (2026-06-16)
### 버그수정 — BTC 1σ/2σ 봇수 추정식 과대평가 수정 (핵심)
- **문제**: `봇수 = range_pct / 매수간격%` 선형 근사식이 범위가 클수록 봇수를 과대평가
  - 예: 1σ 범위 95M~123M(29.5%), 매수간격 0.5% → 선형식 59개
  - 실제로 123M에서 0.5%씩 59회 하락 시 91.8M까지 내려가 하단(95M)을 이탈 — 범위 밖 봇 존재
- **원인**: 그리드는 가격에 곱연산(%)으로 적용되는 기하(geometric) 간격인데, 봇수는 산술(linear) 근사식으로 계산
  - 백엔드 `utils/intraday.py`의 `count_grid_crossings()`는 이미 기하모델(`log(price/box_lower)/log(1+gi%)`) 사용 중 — 프론트엔드만 선형 근사로 불일치
- **수정**: `geoGridBots(u, l, giPct) = ceil(ln(u/l) / ln(1+giPct/100))` 기하모델 도입
  - 1σ 봇수: 59개 → **52개** (123M에서 52스텝 후 94.9M 도달 — 하단 95M 안쪽 정확히 수렴)
  - 2σ 봇수: 164개 → **146개**
  - 1σ 봇당자본: 265,763원 → 301,923원 (상향, 실제 분산 봇수 감소로 자본 밀도 정상화)
  - `btcZoneBots(zone)` 단일 헬퍼로 `updateMetrics()`·`updatePnlTable()` 경고 계산 3개 호출처 통일
- **영향 범위 확인**: 월거래량·순익 추정치(`calcBtcStrat`)는 `BTC_COEF` 캘리브레이션 계수로 자본에 직접 비례 계산되며 봇수와 무관 — **거래량/순익 추정 자체는 이 버그의 영향을 받지 않음**. 영향은 "봇 수"·"봇당 자본" 표시 컬럼과 최소주문 경고 로직에 한정.

## v0.5.5 (2026-06-15)
### 기능 추가 — 하방 집중 배분 시뮬레이터 봇자본 조정 단위 버튼
- **조정 단위 선택 버튼** 5단계: ±1,000원 / ±5,000원 / ±1만원 / ±5만원 / ±10만원
  - 선택된 단위 버튼 강조(USDT 색상 활성화), 나머지 비활성 스타일
  - 기본 단위: 1,000원 (빗썸 최소 조정 단위와 동일)
- **1σ / 2σ 봇자본 즉시 조정 버튼** (− / 현재값 표시 / +)
  - 현재 자본 포맷 표시: 만원 단위 또는 M원 단위 자동 전환
  - `adjustBhCap(zone, dir)` → `usdtSim[zone].cap` 직접 수정
- **연동 검증 완료**: 봇자본 조정 즉시 다음 항목 동기적 갱신
  - 비교표(균등·집중 자본·거래량·순익 차이)
  - 주문가 사다리(봇당 자본 배분 막대)
  - USDT 존별 상세 테이블(봇수·봇당자본·월거래량·순익)
  - USDT 그리드 간격 시뮬레이터 헤더 자본 레이블
  - 슬라이더(자본·비중·KRW예비금) 변경 시 조정 버튼 표시도 자동 갱신

## v0.5.4 (2026-06-15)
### 버그수정 — 미스터리쇼퍼 전수 검수 (하드코딩 → 동적 연동)
- **도넛 중앙 '총 투입자본' 값 하드코딩 수정** (핵심)
  - 중앙 텍스트가 `'40M'`로 고정되어 자본 변경 시 갱신 안됨 → `fmtM(total)`(세그먼트 합계) 동적화
- **BTC 2σ 외부존 폭 24.6% 하드코딩 수정**
  - `btcOuterRange = 24.6` 고정값 → `btcOuterRangePct()` 헬퍼로 실제 σ밴드에서 산출
  - 산식: `((u2a-u1a)+(l1a-l2a))/l1a×100` — backend `box_range_pct`와 동일 기준(l1 분모)
  - 2σ 봇수·봇당자본 경고 2개 호출처 모두 동적 함수로 통일 (변동성 변화에 자동 추종)
- **KRW 예비금 레전드 동적화** — `leg-krw` "별도" 고정 → `KRW 예비금 — XX.XM (총자본 N%)` 실금액 표시
### 검수 결과
- HTML id 260개 ↔ JS 동적 setter 전수 대조: 데이터 셀 전부 동적 갱신 확인
- 4입력(자본·BTC비중·내부존비율·KRW예비금) → 전 존/도넛/레전드/P&L 연동 정상
- BTC→USDT 전 탭 숫자 정합성: 활성률·봇수·거래량·순익 결합모델 일관 반영 확인

## v0.5.3 (2026-06-15)
### 개선 — USDT 주문가 사다리 가독성 향상
- **가격 표시 간소화**: 매 1원마다 별도 행 표시 → 동일 봇자본 구간을 하나의 행으로 묶음
  - 예: `1,425원` 10개 행 → `1,425~1,416원` 단일 행으로 압축 (10배 압축)
- **범위 표기**: 단일가는 `1,425원`, 복수가는 `1,425~1,416원` 형식
- **막대 그래프 유지**: 봇자본 비중 배경 막대·색상·존 경계선 모두 유지

## v0.5.2 (2026-06-14)
### 버그수정 — 전체 화면 수치 연동 정합성 점검
- **BTC 존별 그리드 상세 테이블 연동 누락 수정** (핵심)
  - 월거래량(`~5.6억`/`~1.8억`)·예상순익(`+25.9만`/`+6.7만`)이 id 없이 하드코딩 → 동적 갱신 불가였음
  - `btc-inner-vol`·`btc-outer-vol`·`btc-inner-net`·`btc-outer-net` id 추가, `updatePnlTable()` combined 모델(rows.bi/bo) 결과로 갱신
  - 이제 2σ 봇이 1σ 구간 내 동작하는 효과가 BTC 존별 표에도 정확히 반영
- **BTC 존별 범위 셀 동적화** — `btc-inner-range2`·`btc-outer-range2`, `updateMonitorPanel()`에서 실시간 가격 기반 갱신 (2σ는 "1σ 중첩" 전체범위 표기)
- **봇당자본 경고 계산 불일치 수정** — BTC outer 봇수가 `range_pct/0.3`(외부전용) → `(range_pct+24.6)/0.3`(전체범위)로 `updateMetrics`와 통일
- **연환산 수익률 칩 동적화** — `ann-bi/bo/ui/uo` id 추가, 전략별 net/cap×12 계산 연동 (기존 하드코딩 ~31%/~12%/~43%/~10%)
- **2σ봇 가중 활성률 셀 동적화** — `btc-2s-active`, USDT는 박스폭 연동·BTC는 σ배수 상수
- 전략 평가 카드 문구 수정: "외부존 73% 유휴 대기" → "2σ 봇은 1σ 구간 내에서도 동작" (중복 동작 모델 반영)
### 검증
- 슬라이더(자본·비중·KRW보유) → updateMetrics → updatePnlTable 전 구간 연동 확인
- 최초 로드~파라미터 조정~탭 전환 전체 흐름 수치 정합성 점검 완료

## v0.5.1 (2026-06-14)
### 변경 — BTC/USDT 2σ 봇 전체범위 중복 동작 모델 적용
- **BTC 2σ 봇**: 외부존 전용 → 1σ 구간 포함 전체 2σ 범위 동작
  - σ 배수 기반 내/외부 비중 산출: inner 60.5% / outer 39.5%
  - 가중 활동률 = 60.5%×100% + 39.5%×27% = **71.2%** (기존 27% → 2.6배 향상)
  - BTC_COEF_COMBINED: 기존 outer 계수 × 2.637 보정
  - 봇 수: 외부전용(82개) → 1σ+2σ 전체 범위 (~190개)
- **USDT 2σ 봇**: updatePnlTable · syncUsdtDetail 모두 전체범위 모델 적용
  - 가중 활동률 = **64.1%** (기존 27% → 2.4배)
- **4전략 P&L 수치 변화 (시뮬레이션 결과)**:
  - USDT: 월거래량 3.81억→4.60억 (+21%), 순익 11.0만→13.7만 (+24%)
  - BTC: 월거래량 7.4억→10.35억 (+40%), 순익 38.7만→51.6만 (+33%)
  - 합산 월순익 약 +19만원 증가 (+28%)

## v0.5.0 (2026-06-14)
### 버그수정
- **`calcCombined` 2σ 박스 크기 오류 수정** (Critical)
  - `outer.box`는 이미 상·하단 외부존 합계폭 → 전체 2σ 박스 = `inner.box + outer.box` (61원)
  - 기존: `inner.box + outer.box × 2 = 91원` (50% 과다) → 2σ 봇 수 및 P&L 수치 왜곡
- **`calcCombined` 2σ 봇 활동률 오류 수정** (Critical)
  - 1σ 구간(31원) 내 2σ 봇 active=1.0, 외부 구간(30원) 2σ 봇 active=0.27
  - 가중 평균 active = `(31×1.0 + 30×0.27) / 61 ≈ 0.641` (기존 잘못된 1.0 대신)
- **주문가 사다리 테이블 헤더 텍스트 동적 업데이트** — α 변경 시 2σ/1σ 모두 갱신
- **비교표 차이 열 개선** — 상·하단 봇자본 diff는 1σ 기준 실제 증감, vol/net 정상시나리오는 `─`+툴팁으로 "α 무관" 명시

### 개선 (UX)
- 봇 수 행: `1σ 31개 · 2σ 61개` 형식으로 두 레이어 명시
- 상·하단 봇자본: `1σ색(파랑) / 2σ색(오렌지)` 컬러코드 적용, diff는 1σ 변화분 단독 표시
- 거래량·순익 차이 셀: 정상시나리오 "동일" → `─` + 마우스오버 설명 추가

## v0.4.9 (2026-06-14)
### 개선
- 하방 집중 배분 시각화: **수평 막대 차트 → 수직 주문가 사다리 표**로 교체
  - 커서 없이도 각 그리드 주문가·봇당 자본을 한눈에 확인
  - 균등 배분 vs 하방집중(α) 두 열 나란히 비교, 하방집중 셀에 자본 비율 막대 배경
- **2σ 외부존까지 전체 주문가 표시** (기존: 1σ 내부존만)
  - 1σ/2σ 존을 구분선으로 구분, 각 존 예산을 독립 정규화 (외부존은 봇 수가 적어 봇당 자본 큼)
- **봇당 자본 천원 단위 반올림** — 비교표·사다리표 모두 적용 (직관성 개선)

## v0.4.8 (2026-06-13)
### 개선
- 하방 집중 배분 차트 x축: 봇 레벨(L1~LN) → **실제 주문가격(원)** 표시
  - 박스 상단(고가)부터 매수간격씩 내려가는 각 그리드 주문가가 라벨로 표시
  - 호버 시 주문가 + 봇당 자본 동시 표시
  - 실시간 1σ 박스 수신 시 실제 상단가 기준, 미수신 시 참고가 기준

## v0.4.7 (2026-06-12)
### 변경
- **BTC 봇 자본 배분: 대칭 progressive → 균등 배분으로 변경**
  - walk-forward 백테스트 105개월 결과: 균등이 대칭 progressive 대비 +479만(+21%) 우월
  - 원인: BTC는 상방 편중(하반부 체류 44.9%)으로 박스 중심부 체결이 잦음.
    대칭 progressive는 경계부에 자본을 집중시켜 중심부 체결 효율이 저하됨.
  - `config.py`: `PROGRESSIVE_SIZING=False`, `PROGRESSIVE_ALPHA=0.0`
- BTC에 하방 집중 배분(bottom-heavy)은 불필요 — 적용하지 않음
  (상방 편중 특성상 하방 집중 배분 시 더 불리)

### 추가
- `reports/btc_alpha_sweep.py`: BTC α 최적화 백테스트 스크립트 (105개월)
- `reports/alpha_sweep.py`: USDT α 최적화 백테스트 스크립트 (23개월)

## v0.4.6 (2026-06-12)
### 추가
- **하방 집중 배분 시뮬레이터** (USDT 탭 내 새 섹션)
  - α 슬라이더 (0.0 균등 ~ 1.5 최대 집중): 상단→하단 봇당 자본 선형 점증
  - 균등 배분 vs 하방 집중 비교표: 상단/하단 봇당 자본, 정상/하방편중(70%) 거래량·순익
  - Plotly 막대 차트: 봇 레벨별 자본 분포 시각화
  - 수학 근거: 하방 편중 시나리오에서 거래량 +1,800만원↑ → 리워드 효과 추가
- `config.py`: `USDT_BOTTOM_ALPHA = 0.5` 파라미터 추가
- `agents/grid_optimizer.py`: USDT 하방 집중 배분 로직 적용 (BTC는 기존 대칭 progressive 유지)

### 변경
- USDT 그리드 간격 시뮬레이터 범위 **1~5원 → 1~3원** (실운용 범위 반영)
- USDT 간격 조정 시 봇당 자본 자동 연동 표시 (기존: 묵시적 → 명시적)

## v0.4.5 (2026-06-11)
### 개선
- **종료 버튼 강화**: 클릭 시 서버·"API Server" 터미널 창·런처 창 모두 종료
  - `POST /api/shutdown`: SIGTERM 전에 `taskkill /FI "WINDOWTITLE eq API Server*"` 실행
  - `start.bat`: 런처 창이 API Server 창 소멸 감지 후 자동 `exit`
  - 브라우저 탭 `window.close()` 시도 → 차단 시 "탭을 직접 닫아주세요" 오버레이 표시

## v0.4.4 (2026-06-11)
### 추가
- **자동 업데이트 확인** (`utils/update_check.py`)
  - 서버가 GitHub 저장소의 VERSION을 조회해 새 버전 감지 (30분 캐시)
  - 새 버전 발견 시 대시보드 좌하단(종료 버튼 위)에 `⬆ vX.Y.Z 업데이트` 배지 표시
  - 클릭 → 최신 소스 다운로드·덮어쓰기(data/ 보호) → 서버 자동 재시작 → 페이지 자동 새로고침
  - `GET /api/update/check` · `POST /api/update/apply` 엔드포인트
  - 네트워크 차단/저장소 접근 불가 시 자동 비활성 (배지 미표시)
### 수정
- 종료 버튼이 file:// 로 연 대시보드에서 동작하지 않던 문제 (API_BASE 절대경로 사용)
- CORS allow_methods 에 POST 추가

## v0.4.3 (2026-06-11)
### 추가
- 대시보드 왼쪽 하단에 **서버 종료 버튼** 추가 (`⏻ 서버 종료`)
  - 클릭 → 확인 다이얼로그 → `/api/shutdown` POST → 0.8초 후 SIGTERM으로 안전 종료
  - 종료 중/완료 피드백 표시 (버튼 비활성화 + 메시지 변경)
- `api_server.py`: `POST /api/shutdown` 엔드포인트 추가

## v0.4.2 (2026-06-11)
### 변경
- Windows 전용으로 정리: start.sh / patch.sh 제거, 설치 문서(INSTALL/README/PDF)에서
  macOS·Linux 안내 삭제 — 배포 대상을 Windows로 한정해 패키지 경량화

## v0.4.1 (2026-06-11)
### 개선
- 모니터/BTC 차트: 수정 2σ 상·하단 라인 추가 + 전체 수정 밴드에 호버 수치 표시
- 수정 예측 vs 원본 비교표 확장: 1σ/2σ 상·하단 + Δ(변화율) 열, 상향 빨강/하향 파랑
- 수정 예측 밴드를 비대칭 σ 기준으로 통일 (기존: 대칭 2σ)
- /api/dashboard 응답에 rev_u1/rev_l1 (수정 1σ 밴드) 추가

## v0.4.0 (2026-06-11)
### 추가
- 글로벌 시장 데이터 통합: 원/달러 환율(Frankfurter), 달러 BTC(Binance), 김치프리미엄
- 데이터셋 내보내기에 fx_usdkrw.csv / daily_btc_usd.csv / kimchi_premium.csv 포함
- /api/global 엔드포인트 (환율·달러BTC·김프 현황 조회)
- USDT σ에 환율 EWMA σ 30% 블렌딩 (표본 부족 보완)
- ML σ 보정 특징 9→12개 (김프 수준/30일 변화·환율 30일 σ)
- 데이터셋 시드를 분봉 포함 완전체(20MB)로 교체 — 최초 기동 10초 만에 자동 복원
- patch.bat / patch.sh — 덮어씌우기 원클릭 패치 스크립트
- make_patch.py — 배포자용 패치 ZIP 생성 스크립트

### 변경
- 2σ 밴드 walk-forward 재보정: BTC 1.8/2.0, USDT 1.5/1.65 (독립 상수, 1σ×2 아님)
- 시드 복원 스킵 조건 강화: 일봉+분봉 모두 존재할 때만 스킵
- 프런트엔드 USDT 2σ 표기 ±4.4% → ≈±3.3% 갱신

## v0.3.0 (2026-06-10)
### 추가
- 비대칭 σ 밴드 (BTC: ↑1.1/↓1.2, USDT: ↑1.0/↓1.1)
- 듀얼레이어 자본 배분 (Layer A 그리드 / Layer B DCA / Layer C 역추세)
- ML Ridge σ 보정 (walk-forward 백테스트: 적중 71%→78%)
- Progressive Sizing / Asymmetric TP (ASYM_TP_MULT=2.0)
- 탭 네비게이션 충돌 수정 (duplicate const badge)

## v0.1-0.2
- 초기 구현: BTC/USDT 박스권 예측, 그리드 최적화, 리워드 계산기
